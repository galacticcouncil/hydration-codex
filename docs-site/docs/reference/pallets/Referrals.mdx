---
title: Referrals
description: Runtime pallet documentation
sidebar_label: Referrals
---

# Referrals

Support for referrals, referral codes and rewards distribution.

### Overview

Referrals give an opportunity to users to earn some rewards from trading activity if the trader
used their referral code to link their account to the referrer account.

The trader can get back part of the trade fee too if configured.

Pallet also provides support for volume-based tiering. Referrer can reached higher Level based on the total amount generated by users of the referrer code.
The higher level, the better reward.

Rewards are accumulated in the pallet's account and if it is not RewardAsset, it is converted to RewardAsset prior to claim.

//! ### Terminology

* **Referral code:** a string of certain size that identifies the referrer. Must be alphanumeric and upper case.
* **Referrer:** user that registered a code
* **Trader:** user that does a trade
* **Reward Asset:** id of an asset which rewards are paid in. Usually native asset.

<details>
<summary>Code Documentation</summary>

Support for referrals, referral codes and rewards distribution.

### Overview

Referrals give an opportunity to users to earn some rewards from trading activity if the trader
used their referral code to link their account to the referrer account.

The trader can get back part of the trade fee too if configured.

Pallet also provides support for volume-based tiering. Referrer can reached higher Level based on the total amount generated by users of the referrer code.
The higher level, the better reward.

Rewards are accumulated in the pallet's account and if it is not RewardAsset, it is converted to RewardAsset prior to claim.

### Terminology

* **Referral code:** a string of certain size that identifies the referrer. Must be alphanumeric and upper case.
* **Referrer:** user that registered a code
* **Trader:** user that does a trade
* **Reward Asset:** id of an asset which rewards are paid in. Usually native asset.

</details>

<!-- AI_DESCRIPTION_START -->
<!-- AI_DESCRIPTION_END -->

**Pallet #75** · HydraDX<br/>
`pallet-referrals/4ca470c5/master`

[GitHub](https://github.com/galacticcouncil/HydraDX-node/tree/4ca470c5d613d62ce52eb8645770ef5fff263111/pallets/referrals) · [Subscan](https://hydradx.subscan.io/runtime/Referrals)

## Storage (10)

### ReferralCodes

Referral codes Maps a referral code to an account.

```rust
ReferralCodes: Map<Bytes -> AccountId32> [Blake2_128Concat]
```

### ReferralAccounts

Referral accounts Maps an account to a referral code.

```rust
ReferralAccounts: Map<AccountId32 -> Bytes> [Blake2_128Concat]
```

### LinkedAccounts

Linked accounts. Maps an account to a referral account.

```rust
LinkedAccounts: Map<AccountId32 -> AccountId32> [Blake2_128Concat]
```

### ReferrerShares

Shares of a referral account

```rust
ReferrerShares: Map<AccountId32 -> u128> [Blake2_128Concat]
```

### TraderShares

Shares of a trader account

```rust
TraderShares: Map<AccountId32 -> u128> [Blake2_128Concat]
```

### TotalShares

Total share issuance.

```rust
TotalShares: Value<u128>
```

### Referrer

Referer level and total accumulated rewards over time. Maps referrer account to (Level, Balance). Level indicates current rewards and Balance is used to unlock next level. Dev note: we use OptionQuery here because this helps to easily determine that an account if referrer account.

```rust
Referrer: Map<AccountId32 -> (PalletReferralsLevel, u128)> [Blake2_128Concat]
```

### AssetRewards

Asset fee distribution rewards information. Maps (asset_id, level) to asset reward percentages.

```rust
AssetRewards: Map<(u32,PalletReferralsLevel) -> {
    referrer: Permill,
    trader: Permill,
    external: Permill,
}> [Blake2_128Concat, Blake2_128Concat]
```

### PendingConversions

Information about assets that are currently in the rewards pot. Used to easily determine list of assets that need to be converted.

```rust
PendingConversions: Map<u32 -> Null> [Blake2_128Concat]
```

### CounterForPendingConversions

Counter for the related counted storage map

```rust
CounterForPendingConversions: Value<u32>
```

## Extrinsics (5)

### register_code

```rust
fn register_code(
    origin: OriginFor<T>,
    code: Bytes,
)
```

### link_code

```rust
fn link_code(
    origin: OriginFor<T>,
    code: Bytes,
)
```

### convert

```rust
fn convert(
    origin: OriginFor<T>,
    assetId: u32,
)
```

### claim_rewards

```rust
fn claim_rewards(origin: OriginFor<T>)
```

### set_reward_percentage

```rust
fn set_reward_percentage(
    origin: OriginFor<T>,
    assetId: u32,
    level: Lookup249,
    rewards: Lookup250,
)
```

## Events (6)

### CodeRegistered

```rust
CodeRegistered {
    code: Bytes,
    account: AccountId32,
}
```

### CodeLinked

```rust
CodeLinked {
    account: AccountId32,
    code: Bytes,
    referralAccount: AccountId32,
}
```

### Converted

```rust
Converted {
    from: Lookup500,
    to: Lookup500,
}
```

### Claimed

```rust
Claimed {
    who: AccountId32,
    referrerRewards: u128,
    tradeRewards: u128,
}
```

### AssetRewardsUpdated

```rust
AssetRewardsUpdated {
    assetId: u32,
    level: Lookup249,
    rewards: Lookup250,
}
```

### LevelUp

```rust
LevelUp {
    who: AccountId32,
    level: Lookup249,
}
```

## Constants (6)

### RewardAsset

ID of an asset that is used to distribute rewards in.

```rust
const RewardAsset: u32
```

### PalletId

Pallet id. Determines account which holds accumulated rewards in various assets.

```rust
const PalletId: [u8;8]
```

### RegistrationFee

Registration fee details. (ID of an asset which fee is to be paid in, Amount, Beneficiary account)

```rust
const RegistrationFee: (u32,u128,AccountId32)
```

### CodeLength

Maximum referral code length.

```rust
const CodeLength: u32
```

### MinCodeLength

Minimum referral code length.

```rust
const MinCodeLength: u32
```

### SeedNativeAmount

Seed amount that was sent to the reward pot.

```rust
const SeedNativeAmount: u128
```

## References

### UI (3)

- [useReferralRewards](/reference/hooks/utils/useReferralRewards)
- [useClaimAllWalletRewards](/reference/hooks/wallet/useClaimAllWalletRewards)
- [useWalletRewardsSectionData](/reference/hooks/wallet/useWalletRewardsSectionData)
